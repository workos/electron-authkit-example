# WorkOS AuthKit Configuration
# Get these values from your WorkOS dashboard: https://dashboard.workos.com
#
# electron-vite uses MAIN_VITE_* prefix to expose env vars to main process
# See: https://electron-vite.org/guide/env-and-mode

# Your WorkOS client ID (starts with 'client_')
MAIN_VITE_WORKOS_CLIENT_ID=client_xxx

# Your WorkOS API key (starts with 'sk_')
MAIN_VITE_WORKOS_API_KEY=sk_xxx

# Password for encrypting session data (must be at least 32 characters)
# Generate with: openssl rand -base64 32
MAIN_VITE_WORKOS_COOKIE_PASSWORD=must-be-at-least-32-characters-long-for-encryption

# Note: Configure your WorkOS redirect URI to: workos-auth://callback
